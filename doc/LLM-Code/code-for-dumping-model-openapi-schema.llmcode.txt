from google.cloud import aiplatform
import dark

# Get the model's specs
specs = dark.get_specs()

# Generate the documentation as YAML
documentation = aiplatform.gapic.utils.to_yaml(specs=specs)

# Print the documentation
print(documentation)
